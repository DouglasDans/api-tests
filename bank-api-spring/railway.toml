[build]
builder = "nixpacks"
buildCommand = "curl -s https://get.sdkman.io | bash && source $HOME/.sdkman/bin/sdkman-init.sh && sdk install gradle && ./gradlew build"

[deploy]
startCommand = "java -jar build/libs/seu-projeto-0.0.1-SNAPSHOT.jar"
